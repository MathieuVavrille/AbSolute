init{
  int x = [0;5];
  int y = [0;5];
  int z = [0;5];
  int t = [0;5];
  int a = [0;5];
  int b = [0;5];
}

constraints{
	-3*y + x < 25;
	y + 2*x > 10;
	x + (-y)< -z + 3;
	x - 2*y + -5*z < t - 5 - a + 3*b;
}
